import { defineEnum } from '../utils';

export const FlexJustify = defineEnum({
  START: 'start',
  CENTER: 'center',
  END: 'end',
  BASELINE: 'baseline',
  STRETCH: 'stretch',
  BETWEEN: 'between',
  AROUND: 'around'
});
